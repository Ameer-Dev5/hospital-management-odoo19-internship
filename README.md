# hospital-management-odoo19-internship

Repository cloned into custom_addons/

Basic Linux commands (cd, ls, activate venv) understood

Virtual environment created and activated

Git configured (username, email, push working)

PyCharm project opened with correct interpreter (venv)

Run configuration set (odoo-bin -c odoo.conf)

Odoo server starts successfully without errors

Developer Mode enabled and working

odoo.conf reviewed and understood

No errors or traceback in logs

Screenshots taken (repo, clone, project, venv, run config, server, developer mode, technical menu)